<!DOCTYPE html>
<html lang="es">

<head>
    <title>Reproductor Nativo con Control de Velocidad</title>
    <!-- Bootstrap CSS -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.2.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <!-- Bootstrap Icons -->
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.10.5/font/bootstrap-icons.css">
    <!-- Custom Styles -->
    <link rel="stylesheet" type="text/css" href="/css/styles.css">
    <!-- DateTimePicker CSS -->
    <link href="//cdnjs.cloudflare.com/ajax/libs/bootstrap-datetimepicker/4.17.37/css/bootstrap-datetimepicker.css" rel="stylesheet"/>
    <link rel="stylesheet" href="css/audioscript.css">
</head>

<body class="bg-light">
    <div class="container mt-5">
        <div class="row">
            <!-- Sección de Video y Cortador -->
            <div class="col-md-8">
                <!-- Reproductor de video -->
                <div id="audioContainer" class="mb-4 text-center">
                    <audio id="audioPlayer" controls class="w-100">
                        Tu navegador no soporta el elemento de audio.
                    </audio>
                </div>


                <!-- Cortador de video -->
                <div id="videoCutter">
                    <h3>Cortador de Audio</h3>
                    <div class="row">
                        <div class="col-md-3">
                            <label for="startMinute">Inicio:</label>
                            <input type="number" id="startMinuteAudio" name="startMinute" class="form-control" placeholder="mm" min="0" max="59">
                        </div>
                        <div class="col-md-3">
                            <label for="startSecond">&nbsp;</label>
                            <input type="number" id="startSecondAudio" name="startSecond" class="form-control" placeholder="ss" min="0" max="59">
                        </div>
                        <div class="col-md-3">
                            <label for="endMinute">Fin:</label>
                            <input type="number" id="endMinuteAudio" name="endMinute" class="form-control" placeholder="mm" min="0" max="59">
                        </div>
                        <div class="col-md-3">
                            <label for="endSecond">&nbsp;</label>
                            <input type="number" id="endSecondAudio" name="endSecond" class="form-control" placeholder="ss" min="0" max="59">
                        </div>
                    </div>
                    <div class="mt-3">
                        <button onclick="cutAudio()" class="btn btn-primary">Cortar y Descargar</button>
                </div>
                </div>
            </div>

            <!-- Sección de Navegación -->
            <div class="col-md-4">
                <div id="contentList" style="max-height: 400px; overflow-y: scroll;">
                    <!-- Lista de directorios y videos -->
                </div>
            </div>
        </div>
    </div>

    <!-- Scripts -->
    <script src="https://ajax.googleapis.com/ajax/libs/jquery/3.5.1/jquery.min.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.2.3/dist/js/bootstrap.bundle.min.js"></script>
    <script src="//cdnjs.cloudflare.com/ajax/libs/moment.js/2.15.2/moment.min.js"></script>
    <script src="audioscript.js"></script>

</body>
</html>
